 • Feeds
   • index - destroy, inactive (and sort), created by, # collections, duplicates
   • duplicates - combine with index
   • show - CollectionErrors, CollectionJobs
 • CollectionSummaries
   • index - STYLE, title?, fatal error type and message, jobs, failed, confirm sorting by errors_count works
   • show - CollectionErrors, CollectionJobs
 • ItemCache  
   • index - STYLE, edit/destroy, inline new?, FailedOperations
 • Spiders

 • Remove will_paginate
 • Fix authentication on controller with hmac
